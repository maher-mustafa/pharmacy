import { defineConfig } from "vite";

export default defineConfig({
  base: "/pharmacy/", // Replace with the name of your GitHub repository
  build: {
    outDir: "dist", // This is the default output directory
  },
});
